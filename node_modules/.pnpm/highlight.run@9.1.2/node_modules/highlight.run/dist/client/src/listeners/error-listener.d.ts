import { ErrorMessage } from '../types/shared-types';
export declare const ErrorListener: (callback: (e: ErrorMessage) => void, { enablePromisePatch }: {
    enablePromisePatch: boolean;
}) => () => void;
