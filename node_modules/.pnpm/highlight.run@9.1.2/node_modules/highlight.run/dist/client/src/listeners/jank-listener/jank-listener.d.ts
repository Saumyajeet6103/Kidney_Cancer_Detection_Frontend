export interface JankPayload {
    relativeTimestamp: number;
    jankAmount: number;
    querySelector: string;
    newLocation?: string;
}
export declare const JankListener: (callback: (payload: JankPayload) => void, recordingStartTime: number) => () => void;
